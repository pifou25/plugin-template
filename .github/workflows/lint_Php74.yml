on:
  # manual trigger
  workflow_call:
  push:
    branches:
    - badges
    # only trigger wihen change php files
    paths:
    - "**.php"
  pull_request:
    branches:
    - badges
    - master
    # only trigger wihen change php files
    paths:
    - "**.php"
  
name : 'PHP7.4'
jobs:
  php74:
    # check official jeedom workflows
    # https://github.com/jeedom/workflows/tree/main/.github/workflows
    # https://github.com/PrestaShopCorp/github-action-php-lint
    uses: jeedom/workflows/.github/workflows/lint_Php74.yml@main
